<% if signed_in? %>
  <%= form_for([@product, @product.comments.build], remote: true) do |f| %>
  <div class="row" id="distance">
  <div class="col-md-4"> 
  <p><%= f.label :body, "Comments" %><br><%= f.text_area :body %></p></div>
       <p>
    <div class="rating"></div>
  </p>
  <div class="col-md-4"> <p>
      <%= f.submit "Submit" %>
    </p></div>
</div>
    <% end %>
<% end %>